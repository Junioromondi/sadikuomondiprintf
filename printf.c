#include <stdio.h>
#include "main.h"
/**
 *
 *
 * 
 */
int _printf(const char *format, ...)
{
	int i;

	if (format[0] == '%')
	{

	}

	for (i = 0; format[i] != '\0'; i++)
	{
		count ++;
	}
	return (count);
}
int main(void)
{
	const char str[];
	
	str = "cs";

	_printf("c);
	return (0);
}
